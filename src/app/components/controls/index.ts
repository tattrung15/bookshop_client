import Input from "./input.control";
import Select from "./select.control";
import Button from "./button.control";

const Controls = {
  Input,
  Select,
  Button,
};

export default Controls;
